namespace DriverFinder;

public class DriverAccept
{
    // for automatic driver accepting
    public static bool AutomaticDriverRideAccept()
    {
        return true;
    }
    
    // add manual diver accept here (connected to ui)
}